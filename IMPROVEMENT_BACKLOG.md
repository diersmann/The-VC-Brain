# The VC Brain - Improvement Backlog

Status: open backlog
Audit date: 2026-08-07
Audited branch: main at 4edc3b0
Comparison base: origin/main at 4edc3b0

## Purpose

This is an implementation backlog for the next coding agent. It is grounded in the current code, the nine-page challenge brief, docs/ARCHITECTURE.md, local validation, and live browser QA at desktop and mobile widths.

The main conclusion is simple: make the product truthful before making it broader. Several screens and lifecycle events currently present drafts, fallbacks, missing values, or operational failures as completed investment work. The first milestones should make states, scores, evidence, outreach, memos, and SLA claims precise and auditable.

## How to use this backlog

- Priorities are P0 (trust, safety, or core-contract blocker), P1 (next complete product milestone), P2 (quality, UX, and production readiness), and P3 (differentiated learning features).
- Evidence labels: Observed means directly reproduced or read in code; Missing means a required capability was not found; Inferred means a concrete risk follows from the implementation and should be verified while implementing.
- Before taking an item, inspect its evidence and dependencies, then update this file with owner/status or link the replacement issue.
- Preserve the hard project rules: Founder Score is person-scoped; Market and Idea-vs-Market are opportunity-scoped; Trust Score is claim-scoped; missing evidence lowers confidence, not founder quality; network signals aid sourcing, not merit.
- A backlog item is not done until behavior, tests, migrations, documentation, and observability are updated together.

## Recommended delivery sequence

1. Truth and safety: VCB-001 through VCB-018.
2. Evidence and lifecycle foundation: VCB-019 through VCB-035, VCB-051 through VCB-057.
3. Fair sourcing and a complete investment workflow: VCB-036 through VCB-050, VCB-058 through VCB-060.
4. Investor-grade UX and engineering quality: VCB-061 through VCB-101.
5. Learning and differentiation only after trustworthy data exists: VCB-102 through VCB-110.

## P0 - Product truth, security, and data integrity

- [x] **VCB-001 - Remove the fake application path and keep one real submission flow.** Observed: App.tsx:19 exposes /apply; InboundPage.tsx:5-20 discards all form values, does not require a deck, and shows success for hard-coded Aperture AI. Done when /apply redirects to or shares the real /submit implementation, a failed request can never show success, and an integration test verifies the persisted opportunity ID and uploaded deck.

- [blocked] **VCB-002 - Add authentication, fund/tenant scoping, and RBAC.** Observed: main.py:22-32 mounts all routers without auth; candidates.py:95-115 exposes email, handles, and consent; unauthenticated callers can decide, contact, collect, merge identities, and change the active thesis. Done when only health and a rate-limited public submission endpoint are anonymous, every internal route enforces role and tenant scope, and 401/403 coverage exists. **Blocked:** safe implementation requires decisions on the authentication provider/session or token contract, role/permission matrix, whether canonical founder/evidence records are global or fund-private, public submission tenant-routing/intake tokens, and mapping existing data/system actors to an initial fund. Partial route guards would still permit cross-tenant leakage because domain records and background jobs have no tenant scope.

- [x] **VCB-003 - Fix FastAPI database-session lifecycle.** Observed: db/session.py:23-28 returned an AsyncSession rather than yielding it from a closing context. Done when requests close sessions deterministically, roll back on exceptions, and a concurrency test proves checked-out connections return to baseline. Completed with `session_context`, the FastAPI generator dependency, direct-route migration, and `backend/tests/test_session.py`.

- [x] **VCB-004 - Harden untrusted pitch uploads before storage or parsing.** Observed: inbound.py:23-48 read the full upload into memory with no byte, MIME-signature, filename, page, encryption, malware, or decompression limits; inbound_job.py:42-53 parsed it inside the privileged worker. Done when uploads stream to quarantine, are validated and scanned, parsing is resource-limited/isolated, and abuse tests cover oversized, polyglot, encrypted, malformed, and bomb files. Completed with PDF quarantine, configured scanner fail-closed policy, bounded worker extraction, and abuse coverage in `backend/tests/test_inbound_submission.py`.

- [x] **VCB-005 - Make inbound submission transactional and idempotent.** Observed: inbound.py:74-85 committed before Redis enqueue, leaked a new Redis pool, and had no idempotency key; retries could create duplicate opportunities or persisted work that never ran. Done when one DB transaction writes the application and durable outbox, duplicate client retries resolve to the same submission, and dispatch is recoverable after Redis outages. Completed with InboundSubmission, OutboxEvent, migration 009, retryable worker dispatch, and frontend key reuse.

- [blocked] **VCB-006 - Converge activated outbound submissions into the same opportunity.** Observed: outreach sends a generic `/submit` link; inbound creates a new opportunity; `lifecycle_job.py:289-295` intentionally leaves the outbound record at contacted. **Blocked:** signed expiring activation tokens, authenticated founder identity, ambiguous-match review, and a safe opportunity/thesis/channel binding contract are not available; silently matching by email would risk cross-opportunity disclosure.

- [x] **VCB-007 - Require explicit opportunity IDs for every opportunity-scoped job.** Observed: opportunity_service.py:21-54 returns the latest opportunity for a person; memo_job.py:110-115 may create/reuse an outbound opportunity; collectors/jobs.py:998-1002 drops the inbound opportunity ID. Done when research, assessment, memo, and decision work reject ambiguous person-only invocation and multi-company tests prove isolation. **Completed:** dispatcher propagation now passes `opportunity_id` to research and memo jobs; both jobs reject missing or non-linked IDs instead of selecting/creating a person’s latest opportunity. Manual research, memo generation/read, and decision APIs bind to explicit opportunities; lifecycle and inbound queue payloads preserve the ID. Candidate mapper isolation tests cover repeat-founder market-axis leakage.

- [x] **VCB-008 - Replace simulated outreach with honest delivery states.** Observed: lifecycle_job.py:268-287 marked contacted before work; contact_job.py:60-104 stored an outreach_email_draft and wrote Cold outreach sent without any mail-provider call. Done when drafted, approved, send_requested, sent, delivered, bounced, replied, opted_out, and failed are distinct; only provider-confirmed send changes delivery state; first contact requires human approval and suppression checks. Completed with persisted OutreachMessage state, separate approval/send-request endpoints, suppression checks, and provider-transition tests; automated contact now creates a draft without claiming delivery.

- [x] **VCB-009 - Separate Founder Score, sourcing signal, thesis alignment, and opportunity assessments.** Observed: candidates.py:226-254 merges newest components across every person-scoped rubric; Tavily stores Market and Idea-Market in person snapshots. Done when each artifact has one subject type and schema, and no Market or Idea-Market value can leak across a repeat founder's companies. **Completed:** person snapshots now contribute only person-scoped score artifacts; canonical opportunity-axis snapshots use `subject_type=opportunity` and are joined to the current opportunity before API mapping. Legacy person-scoped market values are ignored, preventing cross-company leakage. Mapper regression coverage added.

- [x] **VCB-010 - Implement the canonical independent three-axis assessment contract.** Observed: scoring_job.py:88-108 persists execution, technical, and commercial as Assessment axes, while the product requires Founder, Market, and Idea-vs-Market. Done when specialist dimensions feed the persistent Founder Score, a separate opportunity assessor emits exactly the three required axes, and API/UI/tests use one versioned ontology without averaging them. **Completed:** specialist execution/technical/commercial outputs remain inside the person-scoped Founder Score; Tavily research persists exactly Founder, Market, and Idea-Market `Assessment` rows plus an opportunity-scoped `opportunity-axes-v1` snapshot. Candidate APIs expose Market/Idea-Market only from the current opportunity. Architecture and regression tests updated.

- [x] **VCB-011 - Treat missing evidence as unknown, never bearish or zero.** Observed: collectors/jobs.py:676-709 assigns 0.30 and Bearish when Tavily returns nothing; candidateProfile.ts:14-20 and 131-133 convert null scores to zero. Done when absent evidence yields neutral/unknown with low confidence and explicit gaps, and cold-start regression tests prove it cannot reduce founder quality. **Completed:** zero-result research now scores neutral (`0.50`) with zero confidence, the existing axis-gap builder keeps explicit diligence unknowns, and Founder profile axis cards retain null/pending values without a fake zero trend. Added backend cold-start/rating coverage and frontend profile regression coverage.

- [x] **VCB-012 - Generate memos only from accepted claims for the exact opportunity and pinned thesis.** Observed: memo_job.py:47-108 consumed raw observations, person-wide assessments, and the currently active thesis. Done when memo inputs are immutable accepted-claim and assessment snapshots for one opportunity/thesis version, with contradictions and unavailable data surfaced inline. **Completed:** Migration 012 records opportunity provenance on observations/claims and claim/assessment input IDs on memos. Reconciliation and deduplication isolate repeated predicates by opportunity; memo generation accepts only supported or Tavily-synthesized, non-superseded claims whose observations resolve to the exact person/opportunity, pins the opportunity thesis version, scopes assessments, and includes contradiction/unavailable context. Added provenance, memo-input, and cross-opportunity isolation tests.

- [x] **VCB-013 - Validate memo citations and sentence support server-side.** Observed: memo.py:143-153 accepted arbitrary model-returned evidence IDs; memo_job.py:129-143 unioned them with every observation and truncated an unordered set. Done when every citation is membership- and scope-checked, factual sentences map to supporting claim IDs, unknown IDs fail validation, and citation order is deterministic. **Completed:** Memo sections now require claim IDs alongside evidence IDs; production generation passes the exact accepted-claim/observation allow-lists, rejects unknown citations and uncited factual text, normalizes citation order, and persists validation errors with failed status. Added schema and validator regression coverage; frontend types mirror claim citations.

- [x] **VCB-014 - Do not let fallback content make an opportunity memo-ready.** Observed: memo.py:77-89 returns placeholder sections when the model is unavailable; the row is persisted and lifecycle_job.py:297-304 treats any memo as ready. Done when memo runs have pending/failed/degraded/succeeded status and only a validated successful memo satisfies the lifecycle gate. **Completed:** Added memo status persistence and migration 011; pending runs are committed before model execution, failures/fallbacks remain failed or degraded, incomplete model sections fail validation, and lifecycle advancement now requires `succeeded`. The API and decision UI label non-ready drafts explicitly. Added status and lifecycle tests; architecture docs updated.

- [blocked] **VCB-015 - Route decisions through the lifecycle domain service with real actor attribution.** Observed: candidates.py:708-743 mutated the latest opportunity directly, permitted any source state, and hard-coded vc-ui:sophie-werner. **Partial completion:** decisions now require an idempotency key, lock the exact opportunity row, enforce lifecycle transitions, require a succeeded memo, and persist memo claim/assessment/thesis snapshot metadata; migration 013 adds the durable key and the frontend sends it. **Blocked:** final completion requires the VCB-002 authentication/session contract and actor/role mapping so the hard-coded actor can be replaced with an authenticated principal. Required decisions are the auth provider/token contract, role matrix, and initial system actor mapping.

- [x] **VCB-016 - Make reconciliation idempotent and define one supersession direction.** Observed: reconcile.py:50-131 created new claims on every run; contradiction and dedup code used opposite supersession semantics; valid_time_end was never closed. Done when unchanged inputs create zero rows, superseded_by semantics are consistent, prior validity closes atomically, and current truth is deterministic. **Completed:** reconciliation reuses deterministic claim outputs, scopes the idempotency key by predicate/opportunity/observations, and closes prior claims by pointing their `supersession_id` to the newer replacement with `valid_time_end`. Deduplication now follows the same older-to-newer direction and closes validity. Added repeated-predicate opportunity isolation and reconciliation regression coverage.

- [blocked] **VCB-017 - Make identity resolution conservative, auditable, and reversible.** Observed and reverified: matchers.py:193-212 auto-merges fuzzy name plus company/location at 0.85; merge.py:84-125 rewrites evidence in place, omits Claim/InboundSubmission/OutreachMessage/PersonMatch dependents and collision handling, and deletes self-relationships. **Blocked:** safe completion requires an immutable identity-alias/merge-event model, a policy for duplicate opportunity/relationship join rows, coverage of every person-dependent artifact, and a tested rollback/retention contract. Disabling all auto-merges or patching one dependent would leave provenance and recovery semantics unresolved; fuzzy matches must remain review-only until that design is approved.

- [x] **VCB-018 - Never present operational failure as valid investment data.** Observed in live QA: HomePage.tsx:11-30 turns API 500s into a healthy zero pipeline/no thesis; PitchSubmissionPage.tsx:21-29 reduces submit failure to an alert and returns to an apparently normal form. Done when loading, empty, partial, offline, permission, and failure states are distinct, persistent, accessible, retryable, and never mutate or imply investment conclusions. **Completed:** typed API failures now distinguish permission, offline, server, and request errors; Home and Investigated keep failed data from becoming zero-valued metrics and expose persistent accessible retry notices; empty thesis/pipeline states are explicit; submission failures remain inline and retryable. Added API failure, dashboard failure/empty/partial/retry tests. Frontend lint, typecheck, 33 tests, and production build pass. `make check` was attempted at the milestone boundary but remains blocked by the pre-existing unavailable Docker daemon.

## P1 - Complete the trustworthy product loop

- [blocked] **VCB-019 - Enforce append-only evidence and audit history.** Observed and reverified: evidence/audit FKs still permit cascade deletion and the importer previously updated observations in place. **Partial completion:** `import_public_inbound.py` now preserves existing observations and appends a correction observation with provenance when values change, while linking public observations to their exact opportunity. **Blocked:** full completion requires database roles/triggers that deny normal mutation/deletion across evidence and audit tables plus an approved privacy-erasure/tombstone and retention contract. The current repository has no role-management or lawful-erasure policy to implement safely.

- [x] **VCB-020 - Implement a real per-claim Trust Score.** Observed: Claim stored only a scalar confidence; candidateProfile.ts:53-73 relabeled confidence as Trust. Done when versioned inputs include source authority, directness, independence, corroboration, freshness, extraction and identity confidence, contradiction penalties, explanation, status, and calibrated interval. **Completed:** Migration 014 adds versioned Trust score, interval, components, and explanation fields while preserving extraction confidence. Reconciliation computes conservative claim Trust with source authority, directness, independence, corroboration, freshness, extraction confidence, explicit unknown identity confidence, and contradiction penalty; legacy rows are labeled `legacy-unknown`. Candidate API/UI now use the dedicated Trust score. Added reconciliation Trust coverage.

- [blocked] **VCB-021 - Model contradiction sets without silently choosing a favorable winner.** Observed and reverified: reconcile.py:23-29 ranks conflicting observations by confidence/recency and treats values as timeless competitors. **Blocked:** completion requires a product policy for temporal-change windows versus same-time conflict, an explicit reviewer/resolution authority, and a durable contradiction-resolution artifact that can be linked to exact claims. VCB-020 now applies a contradiction penalty and preserves counter-claims, but selecting or resolving a winner without those decisions would encode an unsafe investment policy.

- [x] **VCB-022 - Preserve source coordinates end to end.** Observed: inbound_job.py concatenated pages and truncated at 50k; observation/claim DTOs omitted page, slide, DOM fragment, or text span. Done when every claim can open the immutable source at its page/slide/timestamp/span and unavailable content still exposes provenance metadata. **Completed:** Migration 015 adds observation source locators; bounded PDF extraction now emits one page observation with PDF page and character-span coordinates, including an explicit no-text marker. Candidate API/frontend types expose the locator, and claims retain the observation reference. Added PDF coordinate coverage.

- [x] **VCB-023 - Make advertised deck formats match actual parser support.** Observed: PitchSubmissionPage.tsx:169-175 accepted PDF/PPT/PPTX while inbound_job.py always used pypdf. Done when the product is PDF-only with server enforcement or safely parses every advertised format with coordinates and format-specific tests. Completed alongside VCB-004: the form advertises PDF only, the server rejects non-PDF filenames/content, and format rejection is tested.

- [blocked] **VCB-024 - Orchestrate inbound through triage, Founder Score, three axes, diligence, memo, and decision.** Observed: inbound_job.py:55-101 reconciled/embedded/deduplicated then queued a memo without scoring or opportunity assessment. **Partial completion:** inbound processing now stops after the evidence-processing stage; the lifecycle worker records the missing inbound-triage outputs, queues scoped research plus Founder Score plus processing, and only queues memo generation after scoped accepted claims, Founder Score, and all three opportunity axes exist. Focused tests cover both prerequisite and memo queue gates. **Blocked:** full completion still requires a durable per-stage run/output ledger with explicit failure/gap states, authenticated diligence ownership, and the canonical lifecycle/decision contract.

- [blocked] **VCB-025 - Implement the real 24-hour SLA clock.** Observed: Opportunity lacked received_at, decision_due_at, stage deadlines, owner, or pause reason; DecisionDetail showed Not scheduled. **Partial completion:** Migration 020 adds the persisted receipt clock, 24-hour due time, cumulative architecture stage deadlines, owner/pause fields, and decision attainment. Inbound creation and outbound-to-received transitions initialize the clock idempotently; human decisions persist met/breached attainment and SLA metadata on the decision event. Candidate APIs now expose countdown, stage, warning/breach state, and unassigned-owner truth, and Decision Detail renders the live state instead of a fabricated schedule. **Blocked:** full completion still requires authenticated owner assignment, the canonical stage machine and stage-specific pause/resume transitions, alert delivery, and persisted p50/p90 SLA metrics/attainment reporting.

- [x] **VCB-026 - Align one canonical lifecycle across domain, jobs, API, and workflow diagram.** Completed: `backend/app/lifecycle.py` now publishes the versioned `unified-v2` state machine with stage entry/exit requirements, actors, transitions, and timestamp provenance; domain status validation derives its stage vocabulary from that contract; `GET /api/v1/lifecycle` exposes it; inbound jobs advance through `received -> triage -> screening -> diligence -> memo_ready`; and the workflow diagram renders labels from the API contract. Contract, worker, API, and UI tests cover the shared source. Provider-confirmed outreach transitions and authenticated ownership remain tracked by VCB-028/VCB-025 rather than being hidden in this vocabulary contract.

- [blocked] **VCB-027 - Persist a structured decision proposal and readiness snapshot.** Observed: DecisionDetailPage.tsx:555-593 synthesized Proceed/Hold/Investigate and narrative locally; backend decision accepted only action/reason. **Partial completion:** Migration 021 adds a versioned `DecisionProposal` artifact with action, nullable check amount/ownership/conviction, exact Founder/Market/Idea-Market assessment foreign keys, memo claim evidence, assessment-derived risks/conditions, readiness blockers, thesis/model/rubric versions, and override reason/status. Memo generation persists the proposal, candidate detail exposes it, and the decision endpoint records an override when human action differs; the decision UI now consumes persisted proposal recommendation, risks, and conditions. **Blocked:** investor-entered check amount/conviction/ownership, complete rubric/prompt provenance, and authenticated actor/override semantics require the contracts tracked by VCB-002/VCB-034.

- [blocked] **VCB-028 - Make reviewed outreach a first-class workspace.** Observed: InvestigatedPage.tsx:14-21 immediately calls /contact while an OutreachComposer exists elsewhere. **Partial completion:** Investigated contact now opens the editable OutreachComposer; it shows origin/source types and consent, blocks suppressed/no-email handoff, requires an explicit approval checkbox before opening an email client, and labels the handoff as unconfirmed provider delivery/reply state. **Blocked:** persisted draft identity, server-side approval/send state, and provider delivery/reply webhooks require the authenticated outreach contract and provider integration.

- [blocked] **VCB-029 - Gate external AI use with privacy purpose and PII minimization.** Observed: scoring.py:422-427 sent email/handles and raw deck observations while consent defaulted to pending. **Partial completion:** Migration 022 models legal basis, notice version, per-purpose grants, provider policy version, retention days, and residency; `privacy-ai-v1` denies external calls for pending/unknown/suppressed or incomplete policy, redacts email/phone identifiers in AI evidence, and gates scoring, research, embeddings, memo, and outreach paths with local fallback where safe. **Blocked:** authenticated consent/rights-management workflows and production provider retention/residency configuration remain part of VCB-002/VCB-030/VCB-031.

- [blocked] **VCB-030 - Add correction, export, deletion, retention, and outreach-suppression workflows.** **Partial completion:** privacy purpose/legal-basis fields, source-use controls, append-only corrections, and suppression-aware AI/outreach gates exist. **Blocked:** authenticated rights requests, global/channel suppression, retention jobs, correction review, export/delete propagation, and audit actor/tenant mapping require the missing auth and lawful-erasure contract; implementing deletion now could violate evidence-retention obligations.

- [blocked] **VCB-031 - Enforce source-use and licensing policy.** Observed: `SourceSnapshot.license_metadata` was nullable and no central connector policy gate existed. **Partial completion:** Migration 023 adds versioned `source-use-v1` policy metadata with collection, retention, display, model, contact, and export decisions; connector license hints remain deny-by-default for model use; founder-provided decks and internal outreach are explicitly allowed; unknown/disallowed connector snapshots are retained but observation writes and scoring inputs are quarantined. **Blocked:** source-owner/legal review and connector-specific production licenses are required to populate explicit allowed-use grants across every source.

- [blocked] **VCB-032 - Normalize Organization, Opportunity, and founder-role relationships.** Observed: Opportunity stored `company_name` instead of an Organization FK; Organization was minimally used. **Partial completion:** Migration 024 adds nullable Opportunity→Organization linkage and temporal founder `role`/`valid_from`/`valid_to` fields; new inbound/outbound opportunities resolve deterministic organization identities and candidate detail exposes the organization ID. **Blocked:** historical organization backfill/merge review, multi-founder API workflows, and moving company research off person-scoped observations require identity/auth contracts.

- [blocked] **VCB-033 - Replace polymorphic UUID/JSON references with enforceable evidence links.** Observed: Observation.subject_id, Claim.subject_id, ScoreSnapshot.subject_id, observation_ids, and evidence_ids had little or no referential integrity. **Partial completion:** Migration 025 adds the FK-backed `claim_observations` join table and reconciliation writes typed links alongside the legacy JSON list, so orphan claim/observation rows are rejected. **Blocked:** historical JSON backfill, typed subject union tables for observations/scores, and database-level wrong-scope enforcement require a broader evidence contract and migration review.

- [blocked] **VCB-034 - Version every derived artifact and model invocation.** Observed: Assessments lacked rubric/prompt/model/input package metadata; score metadata returned by agents was not fully persisted. **Partial completion:** Migration 026 adds a shared `artifact_metadata` contract to claims, relationships, scores, assessments, memos, and proposals; reconciliation, thesis/agent scoring, research, memo, and proposal writers now persist run IDs, code/rubric/prompt/model versions, parameters, input fingerprints, latency where measured, validator status, and compatibility metadata. **Blocked:** historical backfill, true provider cost accounting, and relationship-producing flows require an execution/run ledger and connector billing telemetry.

- [x] **VCB-035 - Treat validator failure as failure and verify agent citations deterministically.** Observed: scoring.py returned an empty critique on missing key/API/JSON failure and aggregation interpreted it as no hard issues. Done when validator_status is explicit, threshold actions require successful validation, and cited IDs/support are checked outside the LLM. **Completed:** specialist and critic outputs now carry passed/failed/unavailable validation states and errors; unknown evidence citations are rejected against the immutable evidence block; deterministic aggregation sets `hard_eligible=False` unless every validator passes, and persisted Founder Score metadata includes the validator result. Added fallback/valid committee regressions.

- [blocked] **VCB-036 - Build a visible natural-language sourcing query planner.** Observed: SourcingPage sends a compound default query to hard-coded GitHub; github.py:54-65 interprets the entire string as a location. **Partial completion:** the Sourcing screen now parses role, geography, sector, traction, exclusion, freshness, graph, and unparsed clauses into a visible plan, identifies the geography forwarded to GitHub, and warns when a location is missing or other intent remains downstream-only. **Blocked:** source-specific query compilation and per-result clause explanations require a backend discovery-plan contract and connector support beyond the current single opaque query string.

- [blocked] **VCB-037 - Stop ranking candidates by averaging the three axes.** Observed: SourcingPage.tsx:57-64 averages Founder, Market, and Idea-Market despite the product contract. **Partial completion:** sourcing now ranks complete profiles by the lowest independent axis, keeps incomplete profiles in a discovery-signal queue without assigning zero merit, and shows the ranking rationale above each card. **Blocked:** a thesis-specific floor/material-risk policy and offline evaluation still need product ownership and labeled ranking outcomes.

- [blocked] **VCB-038 - Use a versioned activation priority model.** Observed: lifecycle gates mostly on signal composite/founder aggregate. **Partial completion:** `activation-priority-v1` now computes a bounded, weighted activation score across thesis fit, novelty, momentum, evidence confidence, identity confidence, contactability, deadline pressure, cost efficiency, and exploration quota; missing inputs are renormalized and surfaced, and discovery deep-collection queueing uses the model when evidence coverage is available. **Blocked:** lifecycle/SLA scheduling integration, thesis-specific floor/material-risk policy, exploration quota ownership, and labeled offline evaluation require product-owned policy and outcomes.

- [blocked] **VCB-039 - Add a real cold-start evidence path.** Missing: structured work samples, prototypes, references, interviews, and learning-velocity inputs. **Partial completion:** the inbound submission now accepts bounded optional work-sample URL/description, learning-velocity, reference-context, and interview-context fields; Migration 027 stores them and inbound processing converts them into opportunity-scoped founder-evidence observations. The UI states that missing public history does not lower founder quality. **Blocked:** authenticated founder correction/work-sample review, repeat milestone history, and comparable rubric/evaluation calibration require the founder-facing identity and analyst-review contracts.

- [blocked] **VCB-040 - Add fairness evaluation and scoring guardrails.** Observed: public stars, votes, citations, and appearances shape discovery; arXiv hard-filters by citation count. **Blocked:** the repository has no lawful, consented subgroup labels, outcome/advancement labels, shadow-ranker evaluation surface, or approved proxy-sensitive feature policy. Protected characteristics must not be inferred or added to merit scoring; implementing metrics without those inputs would be misleading. Required next decision: approve cohort definitions, measurement horizon, proxy policy, and shadow-mode owner before engineering the evaluator.

- [blocked] **VCB-041 - Make collection persistence idempotent.** Observed: collectors/jobs.py:132-187 inserted new snapshot and observation rows even when MinIO detected identical content. **Partial completion:** collection now reuses the same URI/source/content-hash snapshot and skips identical observation rows, computes unambiguous deterministic persistence fingerprints, and catches unique-key races inside a savepoint before re-reading the winning row. Migration 031 adds nullable fingerprint columns and partial unique indexes; it backfills only one deterministic representative per natural-key group and preserves legacy duplicates as NULL rather than deleting or rewriting evidence. **Blocked:** full enforcement still requires an approved duplicate-resolution/retention policy, historical duplicate review, and a safe path to make all fingerprints non-null; current constraints protect new fingerprinted writes while legacy duplicate rows remain explicitly outside the unique index.

- [blocked] **VCB-042 - Validate observation schemas at ingestion.** Observed: empty values, arbitrary confidence, naive string dates, and missing coordinates could be inserted. **Partial completion:** collection ingestion now normalizes timestamps to UTC, rejects future/invalid times, rejects empty predicate/value and non-finite/out-of-range confidence, and attaches an explicit coordinate-unavailable locator when connectors omit one. **Blocked:** the final durable quarantine requirement depends on a job/error ledger that can retain rejected payload metadata without treating it as investment evidence; that capability is tracked by VCB-051 and is not present yet.

- [blocked] **VCB-043 - Give signal snapshots deterministic provenance.** Observed: signal aggregation used arbitrary historical `setdefault` values and wrote `evidence_ids` empty. **Partial completion:** signal inputs now select the latest valid observation deterministically, retain exact evidence IDs, source confidence, coverage, selected predicates, rejected numeric IDs, and a SHA-256 input fingerprint in versioned provenance. Malformed numeric inputs are excluded and logged. **Blocked:** durable quarantine of rejected numeric payload metadata still depends on the VCB-051 job/error ledger.

- [blocked] **VCB-044 - Treat search snippets and synthesized answers as leads, not trusted claims.** Observed: Tavily results/answers became observations and a nonstandard `tavily_synthesized` Claim that memo generation accepted. **Partial completion:** synthesized answer Claims are no longer created, and memo inputs accept only reconciliation status `supported`; existing `tavily_synthesized` rows are excluded. **Blocked:** the remaining workflow must fetch and verify each original result page before its content can support a claim or assessment; the current research path still uses search output as low-authority lead evidence.

- [blocked] **VCB-045 - Repair Tavily discovery routing.** Observed: `tavily_search` emitted web seeds, `discover_job` ignored seed source type, and then invoked Tavily collect, which raises `NotImplementedError`; entity-name leads were also treated as URLs. **Partial completion:** HTTP(S) URL seeds now route to the `web` collector, discovery-only entity leads are retained and skipped for collection, and seed-source routing is regression-tested. **Blocked:** an end-to-end Tavily discovery test that persists verified web observations requires a configured Tavily credential and external provider access, which are not available for this local validation.

- [blocked] **VCB-046 - Classify entities before founder scoring.** Observed: hackathon projects, YouTube channels, Tavily URLs, and other seeds are all passed through person creation. **Partial completion:** discovery seeds now receive conservative source/entity classifications with confidence, expose per-run entity counts, and only high-confidence person seeds enter Person creation and founder signal scoring; projects, channels, events, unknown URLs, and low-confidence entity leads are excluded from the founder path. **Blocked:** durable Organization/Project/Channel/Event entities, evidence-backed verification, and relationships to people require a domain schema and entity-resolution contract.

- [blocked] **VCB-047 - Fix Product Hunt query and cursor semantics.** Observed: the GraphQL query variable is unused and pagination fabricates an offset instead of provider cursor. **Partial completion:** the discovery query now applies the input as the documented Product Hunt topic filter, follows `pageInfo.endCursor`, returns the requested page without fabricated offsets, fails fast for invalid pages, and has mocked multi-page no-skip coverage. **Blocked:** the shared connector/job contract still accepts only a page number, so reusable cursor state and provider-specific continuation metadata require a broader discovery-event contract.

- [blocked] **VCB-048 - Remove citation count as an arXiv eligibility gate.** Observed: arXiv discovery dropped authors below the configured citation minimum. **Partial completion:** all authors in the relevant query results are now discoverable regardless of citation count; counts remain explicit `context_only` seed metadata and downstream collection already persists aggregate citation observations. **Blocked:** cold-start coverage/advancement metrics and a persisted per-discovery citation observation still require the measurement and durable discovery-event model tracked by VCB-051/VCB-059.

- [blocked] **VCB-049 - Make connectors async-safe and block SSRF.** Observed: multiple async connectors called synchronous Tavily SDKs; `website.py` followed arbitrary redirects and buffered arbitrary URLs without private-IP/size checks. **Partial completion:** website collection now accepts only HTTP(S), rejects credentials/local and reserved IP targets, resolves hostnames before connecting, validates every manual redirect, streams up to `APP_WEBSITE_MAX_BYTES`, and runs Tavily extraction in a worker thread. **Blocked:** the same async/thread and SSRF contract remains to be applied and tested across the other Tavily-backed connectors; this checkpoint covers the highest-risk generic web path.

- [blocked] **VCB-050 - Classify connector failures for retries and operator action.** Observed: `collect_job` converted exceptions into successful result dictionaries and several connectors return empty lists on upstream errors. **Partial completion:** collector failures now return explicit `status=failed`, `failure_kind` (`transient`, `rate_limited`, or `permanent`), and `retryable` metadata, with structured logs and classification regression tests. **Blocked:** durable failure persistence, backoff/jitter scheduling, and an operator-visible DLQ require the VCB-051 job ledger.

- [blocked] **VCB-051 - Add a durable job ledger with real job IDs.** Missing: durable status/progress/error/result for collection, research, scoring, memo, parsing, and identity work. **Partial completion:** discovery, research, scoring, and memo queue entry points now create durable `job_runs` rows, return real job IDs, and persist queued/running/succeeded/failed state, attempts, progress, result/error metadata, and `updated_at`; memo degradation is encoded as `result.status=degraded` while its JobRun remains failed. The status endpoint is `GET /collection/jobs/{job_id}`. API queue-entry failures are persisted as failed runs; dispatcher enqueue failures are requeued/refunded and remain queued. Cancellation is model-supported but has no implemented setter or workflow yet. **Blocked:** processing, parsing, identity, and avatar jobs still do not have complete ledger integration; cancellation/retry APIs, lease ownership/crash recovery, durable progress semantics, and subscriptions remain open, with transactional queue delivery tracked by VCB-052.

- [blocked] **VCB-052 - Make DB transitions, queue delivery, and budgets crash-safe.** Observed: lifecycle mutates DB/decrements Redis budget/enqueues before final commit; dispatcher popped a task before Arq enqueue. **Partial completion:** dispatcher retains original priority, requeues tasks when Arq enqueue fails, and refunds the Tavily budget reservation on that failure; discovery/research/scoring/memo queue routes now persist job IDs and mark API batch enqueue failures failed; research, scoring, and memo workers use terminal-safe starts that increment attempts and no-op duplicate delivery after succeeded (or model-supported cancelled) terminal rows. **Blocked:** crash-safe leases/ack/requeue, generic cancellation and explicit retry orchestration, deterministic IDs for processing/identity/avatar workflows, and atomic DB/Redis budget reservation still require the broader transactional design.

- [blocked] **VCB-053 - Correct provider/model budget accounting.** Observed: worker startup reset a monthly budget on every restart; tasks are charged rather than actual calls/tokens; manual routes bypass caps. **Partial completion:** Tavily budget initialization is now idempotent across worker restarts, with a regression test. **Blocked:** billing-period reset ownership, actual provider/token usage reconciliation, reservation/refund across every entry point, and visible confidence changes for skipped work require the broader budget ledger design.

- [blocked] **VCB-054 - Prevent historical scores from stalling or satisfying a new opportunity.** Observed: lifecycle used any person-wide `founder-agent-v1` score as the investigation completion gate. **Partial completion:** lifecycle now gates investigation completion on the current opportunity’s `opportunity-axes-v1` snapshot, while treating Founder Score as a person-scoped historical input for the later threshold. **Blocked:** the full contract still needs per-opportunity/run score outputs and deterministic run IDs for research, scoring, and processing.

- [blocked] **VCB-055 - Schedule pipeline work by SLA risk and fairness.** Observed: lifecycle selection repeatedly chose the oldest five; budget-blocked rows could starve later work. **Partial completion:** lifecycle selection now orders by persisted `decision_due_at` with deterministic created/ID tie-breaks, uses PostgreSQL `SKIP LOCKED`, and reports budget-blocked/stuck work through a `needs_attention` count. **Blocked:** stage-deadline ordering, cursor/fair-pagination policy, and a durable operator needs-attention queue require the broader job ledger/SLA ownership contract.

- [blocked] **VCB-056 - Complete the thesis editor and immutable version history.** Observed: OnboardingPage hard-codes name, ownership, risk, exclusions, and weights; backend uses count+1 versioning and rescoring mutates opportunity thesis_version. **Partial completion:** the editor now accepts a visible thesis name and preserves persisted exclusions, ownership, risk, scoring weights, discovery queries, and source-freshness settings when editing an existing thesis instead of silently resetting them. **Blocked:** editable controls for every dimension, race-safe version allocation, history/compare/clone/activate/rollback, and decision thesis pinning require the version-history contract and authenticated ownership model.

- [blocked] **VCB-057 - Make thesis scoring typed, evidence-backed, and benchmarked.** Observed: thesis.py is primarily keyword/rule matching with neutral defaults. **Partial completion:** the scorer returns typed matched/failed/unknown criteria with expected and observed values, explanations, hard-constraint flags, evidence IDs, confidence, and explicit exclusions; a labeled benchmark matrix now guards supported matches, excluded sectors, and missing-evidence neutrality. **Blocked:** distinct semantic similarity and analyst-judgment inputs, a maintained labeled evaluation set, and calibrated precision/recall thresholds require product/model evaluation ownership beyond deterministic unit fixtures.

- [blocked] **VCB-058 - Build one opportunity inbox instead of fragmented person views.** Observed: Inbound, Investigated, and Decisions were separate filters and the candidate DTO lacked a unified workflow view. **Partial completion:** `/inbox` now combines inbound and outbound candidates with canonical lifecycle stage, source types, SLA risk/owner, derived next action/blockers, search, and URL-persisted saved views; unavailable owner data is labeled rather than fabricated. **Blocked:** exact opportunity ancestry, durable assignments, authoritative next-action/blocker DTOs, and server-side saved views require the opportunity inbox API/auth contract.

- [blocked] **VCB-059 - Preserve channel attribution and measure conversion quality.** Missing: first-touch, contributing source/query, outreach, application, and decision conversion are not connected. **Partial completion:** Migration 028 adds append-only `opportunity_channel_touches` with channel, touch type, source query/ref, timestamp, and metadata; inbound applications and outbound discovery writers persist first/contributing touches without using channel as founder merit. **Blocked:** funnel latency, evidence yield, false-positive labels, provider cost, conversion/outcome linkage, and dashboards require the job-ledger and outcome contracts tracked by VCB-051 and VCB-102 through VCB-106.

- [blocked] **VCB-060 - Persist analyst feedback instead of local dismissals.** Observed: `CandidateCard.tsx` hid dismissed cards only in component state. **Partial completion:** dismissal now requires a structured reason, persists append-only `CandidateFeedback` with a separate actor/action audit, and hides the card only after the API succeeds. **Blocked:** save/assign/defer controls, undo/prior-feedback visibility, and authenticated actor identity remain to be integrated.

## P2 - Investor-grade UX, engineering quality, and production readiness

- [blocked] **VCB-061 - Make memo and claim evidence drillable.** Observed: DecisionDetailPage.tsx:444-454 shows only an evidence-reference count; claim/assessment/relationship DTOs omit key provenance. **Partial completion:** candidate detail DTOs now retain observation/claim IDs, and memo references open accessible disclosures with the validated IDs, observed values, source URI/type, observation time, source coordinates, claim status, Trust score/interval, and Trust explanation. **Blocked:** authority ranking, counter-evidence, supersession, derivation metadata, and true source excerpts require expanded evidence DTOs and authenticated analyst workflows.

- [blocked] **VCB-062 - Finish the founder profile's core analytical views.** Observed: candidateProfile.ts builds timeline, coverage, relations, affiliations, and gaps, but FounderProfilePage does not render most of them. **Partial completion:** FounderProfilePage now renders the existing persistent score history, evidence timeline, coverage and gaps, profile context, affiliations/projects, and typed relationships with explicit verified versus needs-verification labels. **Blocked:** correction history, richer relationship provenance, and source-coordinate drill-down require API fields and authenticated analyst workflows not present in the current contract.

- [blocked] **VCB-063 - Replace one-shot timers with durable async progress.** Observed: memo generation refetched once after 5 seconds and research once after 65 seconds; queued state could remain forever. **Partial completion:** discovery, research, and memo screens now consume returned JobRun IDs and poll `GET /collection/jobs/{job_id}` every two seconds only while queued/running; polling stops on recognized terminal statuses (`succeeded`/`failed`, with client handling for model-supported `degraded`/`cancelled`), and terminal success refreshes candidate/memo data. **Blocked:** durable timeout/cancel/retry controls, lease expiry/recovery, and complete polling coverage for processing, parsing, identity, and avatar jobs remain open.

- [blocked] **VCB-064 - Add complete mobile navigation and decision flows.** Observed in live QA at 390px: LeftNav.tsx:14 hides the only navigation and RootLayout supplies no replacement. **Partial completion:** the shell now exposes an accessible mobile menu with all workspace routes, current-route styling, close-on-navigation behavior, and a skip link into the single main landmark; focused render tests cover the menu and shell. **Blocked:** complete sourcing, evidence, memo, and decision task flows still need rendered 320/390/768px device QA and product-approved mobile information density.

- [blocked] **VCB-065 - Fix semantic and dialog accessibility.** Observed: pointer-only article cards, nested interactive inbound rows, a non-keyboard upload div, placeholder-only form labels, an outreach dialog without focus trap/Escape/restore, and nested main landmarks. **Partial completion:** candidate cards now rely on an explicit View Founder button; the outreach dialog focuses its close control, traps Tab/Shift+Tab, closes on Escape, restores focus to its opener, and has focused keyboard regression tests; the shell now has a skip link and one main landmark. **Blocked:** full completion still needs the remaining route semantics and rendered screen-reader/device QA.

- [blocked] **VCB-066 - Replace hard-coded Sophie, actor identity, and 12-of-18 progress.** Observed: LeftNav.tsx:30-35 and HomePage.tsx:35 show fake user/workload values that conflict with live zero data. **Partial completion:** the shell now labels the unavailable session as a Demo workspace, removes the fabricated reviewed-count progress, uses a neutral Pipeline overview heading, replaces the hard-coded decision actor with an unattributed marker, and generated outreach uses a firm-team signature. **Blocked:** authenticated session/workspace data and real actor attribution still require VCB-002’s auth, role, and initial system-actor contract.

- [blocked] **VCB-067 - Correct candidate list semantics and add cursor pagination.** Observed: list query omits canonical filter, uses unsafe concurrent queries on one AsyncSession, stage/origin can match a historical opportunity, and frontend caps at 200. **Partial completion:** origin and lifecycle filters use one deterministic latest-opportunity row per person, person ordering has an ID tie-breaker, lifecycle values are canonicalized at the API boundary, batch reads are sequential on the shared session, and migration 030 adds cursor/filter indexes. The v1 endpoint now supports opaque filter-bound cursors, server-side search, authoritative totals, and a versioned envelope while preserving the legacy array for existing callers. **Blocked:** all consumers still need migration to the bounded contract, and target-scale sorting/filter combinations plus global aggregate views remain open.

- [blocked] **VCB-068 - Make mutations update all affected caches and lock conflicting actions.** Observed: decision action buttons remain usable during save and only detail refetches; Contact/discovery errors are often silent. **Partial completion:** decision actions are disabled while saving; candidate decision, dismissal, research, and discovery mutations now invalidate the legacy, bounded, cursor-paginated list keys and affected candidate detail keys, while queue errors remain visible and retryable. **Blocked:** outreach/lifecycle/thesis mutations still need the same coordinated cache contract, and optimistic/concurrent-conflict semantics plus a shared mutation request/error protocol remain open.

- [blocked] **VCB-069 - Preserve null score semantics throughout the UI.** Observed: missing Founder/Thesis/axis values rendered as 0, null%, or zero meters in profiles, decisions, and Investigated. **Partial completion:** Investigated and shared profile metric cards now render `Not scored` for null values while preserving measured zero as `0%`. **Blocked:** the remaining profile, decision, and meter audit requires broader domain-field coverage beyond the current score and evidence metrics.

- [blocked] **VCB-070 - Separate evidence coverage from confidence.** Observed: `candidateProfile.ts` averaged observation confidence while the UI labeled it confidence-weighted coverage. **Partial completion:** founder-profile calculations now expose separate source confidence and evidence-coverage values, and Founder/Decision views label coverage as breadth rather than quality. **Blocked:** API fields, backend formulas, tooltips, and calibrated model uncertainty still need an end-to-end evidence-metrics contract.

- [blocked] **VCB-071 - Normalize domain statuses at the API boundary.** Observed: backend stores supported lowercase while DecisionDetail checked `Supported` and rendered it as warning; many states/axes/ratings were free strings. **Partial completion:** canonical status literals, legacy-value compatibility, boundary normalizers, DB check constraints, migration 019, and focused backend tests now cover claims, assessment axes/ratings/trends, and lifecycle stages; the frontend maps claim statuses to presentation labels. **Blocked:** every persisted status family (jobs, outreach, identity matches, memo states) still needs separate contracts and UI icon tests before this item can be complete. Existing legacy values are explicitly retained: `tavily_synthesized` claims and `execution/commercial/technical` assessment axes.

- [blocked] **VCB-072 - Distinguish 404, 403, network, and server failures.** Observed: FounderProfile and DecisionDetail treated any fetch error as candidate not found; memo fetch errors became No memo. **Partial completion:** the shared frontend `throwIfNotOk` wrapper now covers API reads and mutations, maps 404/permission/offline/server/request failures, preserves stale candidate data during refetch errors, and keeps memo errors from rendering as an empty memo. **Blocked:** backend error bodies still lack a versioned machine-readable code/retry contract, and route-specific recovery/permission UX remains incomplete even though client-side status typing is now shared.

- [blocked] **VCB-073 - Upgrade search, filter, sort, and result-state UX.** Observed: inbound search only matched founder name/stable ID and KPI totals changed with the filter; no-match said the DB was empty. **Partial completion:** the v1 candidate endpoint now evaluates search server-side across identity, current company, and provenance-backed observations and returns authoritative totals/cursors; the inbound inbox preserves search in the URL, accumulates pages only after an explicit Load more, displays loaded versus total counts, and distinguishes empty data from no results. **Blocked:** richer sort/filter combinations, global KPI aggregates beyond loaded pages, server-side saved views, and migration of every candidate consumer to the versioned contract remain open.

- [blocked] **VCB-074 - Centralize all investment thresholds and labels.** Observed: Investigated called 70 percent strong while shared thesis aligned was 75 percent. **Partial completion:** versioned backend rubric `decision-readiness-v1` now owns 75% thesis-alignment and 60% evidence-confidence thresholds, exposes them with the active thesis, and shared frontend readiness helpers/labels use the same rules. **Blocked:** active-thesis responses are not yet threaded into every cached view, and separate sourcing-signal thresholds still need a distinct rubric contract before all configurable labels can be unified.

- [blocked] **VCB-075 - Render real nullable deadlines and SLA risk.** Observed: DecisionDetail says Decision due in Not scheduled. **Blocked:** this is a presentation dependency of VCB-025; `Opportunity` has no persisted `received_at`, `decision_due_at`, stage budget, owner, or pause reason, and `DecisionEvent.sla_metadata` currently stores action-specific metadata rather than an SLA contract. Implementing countdowns or queue risk now would invent deadlines. **Required:** complete the persisted SLA model and event-derived clock in VCB-025, then expose nullable deadline/risk fields in the opportunity DTO.

- [blocked] **VCB-076 - Show identity verification only when verified.** Observed: FounderProfile always showed a green check next to the founder name. **Partial completion:** the unsupported marker is now omitted. **Blocked:** the API and persistence model expose no explicit identity-verification state, method, source, or timestamp to render when verification is actually complete.

- [blocked] **VCB-077 - Fix misleading workflow navigation.** Observed: Decision feedback/Memory navigated to thesis; Home View all used every candidate but routed to Investigated only. **Partial completion:** Home View all now routes to Discover, which owns the full candidate set, and the Memory node is visibly disabled with an explanation because no separate workspace exists. **Blocked:** a real Memory/feedback workspace and route are still needed for enabled historical decision feedback.

- [blocked] **VCB-078 - Make the public submission form trustworthy and accessible.** Observed: public copy said next cohort, form lacked persistent labels/privacy/support, silent failure relied on alert, and Submit Another retained the previous file. **Partial completion:** `/submit` now uses honest review copy, visible labels and autocomplete, announced submission/file errors, explicit storage/correction guidance, accessible busy state, preserved retry values, and clears the native file input on reset/removal. **Blocked:** durable upload progress/retry status and a real privacy/support destination require a persisted public-submission/job-status contract and approved contact/privacy content; the current endpoint returns only an immediate opportunity ID.

- [blocked] **VCB-079 - Sanitize every external link through one utility.** Observed: several components anchored raw backend URLs despite candidateLinks.ts having a safe URL helper. **Partial completion:** `candidateLinks.ts` now exports strict HTTP(S) validators, rejects unsafe schemes, and CandidateCard, FounderProfile claims, DecisionDetail decks, and the inbound deck link use the shared policy with focused tests. **Blocked:** the remaining `mailto:` outreach action is a deliberate email client action rather than an HTTP external link, while a complete audit still needs a shared non-HTTP action policy and rendered tests for every route-level link.

- [blocked] **VCB-080 - Add route-level error boundaries and a real 404.** Observed: malformed dates could produce invalid localized output; catch-all rendered Coming soon for Not Found. **Partial completion:** date formatting now returns `Unknown date` for malformed timestamps, and the catch-all route has a useful 404 recovery page with a workspace link and tests. **Blocked:** root/route error boundaries and redacted diagnostics still require an agreed client-side error-reporting contract; no telemetry or redaction sink exists yet.

- [blocked] **VCB-081 - Meet contrast, text-size, touch, and mobile information-density standards.** Observed: several muted colors were below AA at 9-12px; mobile lifecycle consumed roughly 2,300px before candidate content; metric detail truncated with a mouse-only title. **Partial completion:** shared muted/eyebrow/data-label colors now have stronger contrast, metric detail wraps, and global reduced-motion preferences are honored. **Blocked:** touch-target, mobile density, and full WCAG AA compliance require rendered-device/manual accessibility QA and a product-approved compact information hierarchy.

- [blocked] **VCB-082 - Unify product and public-facing branding.** Observed: repository/docs say The VC Brain, investor shell says FirstCheck24, public form previously said Fund Application/next cohort, and favicon returns 404. **Blocked:** implementation requires an approved canonical brand name, fund identity, privacy/contact destination, and favicon/mark asset; choosing between the conflicting names without product direction would create a new inconsistency. **Available evidence:** VCB-078 removed the stale cohort/application copy, but the brand decision remains unresolved.

- [x] **VCB-083 - Split oversized React modules by feature responsibility.** Observed: DecisionDetailPage was 594 lines, Onboarding 330, FounderProfile 299, and CandidateCard 266, above the 150-line review limit. **Completed:** the route containers are now 54, 42, 60, and 37 lines respectively; sidebars, choice sections, summary/list sections, evidence/trend visuals, memo, metadata, option helpers, assessment components, card headers, and score/evidence metrics are feature-local, and the existing behavior suites plus focused helper coverage pass.

- [blocked] **VCB-084 - Centralize score, trend, metric, and status display logic.** Observed: similar formatting/fallback/threshold logic is duplicated across cards, profiles, decisions, visuals, and portfolioMetrics. **Partial completion:** typed `displayMetrics` helpers now own ratio/percentage normalization, null-safe labels, profile/decision score visuals, and accessible normalized values; portfolio metrics, candidate profiles/cards, decision visuals, founder profiles, and investigated-page readiness all consume the shared logic/rubric. **Blocked:** full completion still requires a typed view model for trend/status/confidence explanations and an audit of the remaining decision-detail and metric components.

- [x] **VCB-085 - Remove dead code, unused dependencies, and generated config drift.** Observed: unused UI/components and several declared packages; tracked `vite.config.js`/`.d.ts` diverged from `vite.config.ts`. **Completed:** removed the two generated Vite drift files; removed six frontend artifacts confirmed unreachable by repository-wide reference audit (`candidateSignal`, `rubricPercent`, `ScoreBar`, `ApiStatus`, `Chip`, and the superseded `InboundPage`); removed six unused direct dependencies and their lockfile-only transitive packages; and verified clean-container `npm ci` plus the existing CI dependency gate. The remaining npm audit findings are tracked by the security workflow rather than silently bypassed.

- [blocked] **VCB-086 - Lazy-load routes and set a bundle budget.** Observed: App.tsx eagerly imported every route into a 350.5 kB bundle. **Partial completion:** public form, dashboard, sourcing, inbound, investigated, profile, onboarding, and decision routes now load through `React.lazy` with an accessible loading fallback; the build now emits route chunks instead of one eager route module; `make bundle-check` now enforces raw/gzip JavaScript, entry, and CSS budgets and is part of `make check`. **Blocked:** a product-approved performance budget, per-route chunk attribution, and a separate regression trend/report still need CI/product ownership beyond the deterministic gate.

- [blocked] **VCB-087 - Add real integration, E2E, accessibility, and abuse coverage.** Observed: unit tests pass but heavily mock DB/worker paths; major pages have little/no rendered coverage. **Partial completion:** rendered frontend coverage now includes the outreach dialog keyboard contract, the no-opportunity decision guard, mobile navigation/shell semantics, explicit candidate-card navigation, retryable mutation feedback, founder profile insights, memo provenance disclosures, and ranking-policy coverage, with 60 frontend tests passing. **Blocked:** the acceptance scope still needs clean PostgreSQL/Redis/MinIO tests, browser flows, abuse coverage, and authentication/tenant prerequisites; existing route tests use mocks/fakeredis and no MinIO-backed or browser E2E suite exists.

- [blocked] **VCB-088 - Restore and enforce a green CI quality gate.** Observed: no CI workflow; the earlier Ruff/mypy failures are now resolved in the current tree. **Partial completion:** `.github/workflows/quality.yml` now runs Docker-backed `make check`, while `.github/workflows/security.yml` adds dependency, image, secret, and license scans on pushes to `post-hackathon-polish` and pull requests with read-only repository permissions. **Blocked:** clean-database migration smoke, deterministic E2E, and a branch-protection/required-check setting still need CI integration and repository-admin configuration.

- [blocked] **VCB-089 - Make clean quick start migrate and optionally seed automatically.** Observed: README said `make up` on a fresh DB but Compose API did not apply migrations. **Partial completion:** Compose now runs a one-shot `migrate` service before API and worker readiness; `make migrate` uses that same service, and `make seed-demo` adds two clearly labelled, synthetic, deterministic, idempotent local records without network access. **Blocked:** a clean-volume acceptance run and service-failure/readiness assertion still require a dedicated disposable Docker environment or CI integration; the repository now has the wiring and focused fixture tests but this goal does not destroy the existing developer volumes.

- [blocked] **VCB-090 - Make configuration names and Compose wiring coherent.** Observed: `.env.example` mixed `APP_` and unprefixed names; Compose passed only a subset, retained removed `MOCK_REPLY_DELAY`, and many knobs never reached services. **Partial completion:** all backend settings now use the `APP_` convention, API/worker share one complete Compose environment map, the removed mock setting is gone, and bounded numeric settings fail validation at startup. **Blocked:** production secret injection and a full local-versus-container environment matrix still require deployment-specific credentials and CI coverage.

- [blocked] **VCB-091 - Add production-grade health, telemetry, and PII-safe logs.** Observed: readiness checked only PostgreSQL; collection health means registered; no end-to-end correlation. **Partial completion:** readiness now checks PostgreSQL, Redis, MinIO, and a short-lived worker heartbeat; liveness remains dependency-free, failures return 503 without diagnostics, and inspected logs do not emit founder email. **Blocked:** full completion still requires request/job/source/model correlation, queue latency, cost, SLA, connector-health metrics, dashboards/alerts, and a broader PII-redaction audit.

- [blocked] **VCB-092 - Close shared clients cleanly.** Observed: `storage.close_client` existed but neither API nor worker lifecycle called it; Redis queue connections are currently created per request and closed in `finally`, while AI clients are created per invocation without a shared lifecycle. **Partial completion:** API and worker shutdown now close MinIO and database resources, with lifecycle regression tests; existing HTTP clients use context managers and request-scoped Redis clients close deterministically. **Blocked:** full completion requires shared async AI/Redis client ownership and repeated-startup leak tests across all worker and API paths.

- [blocked] **VCB-093 - Harden production images and network defaults.** Observed: backend image runs as root and copies broadly; Compose publishes DB/Redis/MinIO with development credentials and uses minio:latest. **Partial completion:** the backend production image now runs as a dedicated non-root UID/GID, and `make build` now explicitly builds both Dockerfiles’ production targets rather than Compose development stages. **Blocked:** minimal-context cleanup, pinned image digests, internal-only data ports, production secrets/TLS/auth, read-only filesystems/capability drops, resource limits, and separate dev/prod profiles require deployment-specific validation.

- [blocked] **VCB-094 - Add backup, restore, migration, rollback, and incident runbooks.** Missing: production RPO/RTO and tested recovery. **Partial completion:** `docs/OPERATIONS_RUNBOOK.md` now documents local PostgreSQL/MinIO backup and restore commands, migration/forward-fix policy, rolling-worker compatibility expectations, readiness checks, evidence-preserving incident response, ownership placeholders, and explicit recovery-drill acceptance criteria. **Blocked:** production RPO/RTO, scheduled encrypted backups, disposable restore drills, cloud-object retention, rollback rehearsal, and named incident ownership remain deployment-specific and unexercised.

- [x] **VCB-095 - Rewrite stale README and architecture status.** Observed: README said Boilerplate phase and still described adding a router despite implemented routes/features. **Completed:** README and architecture status now distinguish implemented foundations from partial/proposed capabilities, document provider/auth/SLA limitations, provide clean start, opt-in synthetic demo, destructive reset, troubleshooting, and the current workflow route map.

- [blocked] **VCB-096 - Ship a deterministic, visibly labeled demo walkthrough.** Observed: a large seed exists but root onboarding does not expose a one-command demo. **Partial completion:** `make seed-demo` now remains fully synthetic, idempotent, and local while creating two clearly labelled workflow records, one investigating and one memo-ready, with score history, thesis matches, six assessments, a contradiction, evidence-linked claims, and one five-section template memo; seed tests and a live API smoke check verified the expected counts and `/inbound`, `/investigated`, `/decisions`, and `/founders/:id` routes. **Blocked:** the remaining walkthrough contract requires a clean-volume smoke job and current browser screenshots/script coverage for the rendered flow.

- [blocked] **VCB-097 - Add connector contract tests and readiness levels.** Observed: ten registered connectors vary widely in pagination, provenance, retry, and completeness. **Partial completion:** all registered connectors now carry explicit experimental/beta/production readiness metadata with contract-version and limitation fields; a shared collection-output validator rejects empty content, missing provenance, malformed observations, and missing predicate/object fields before persistence, with focused contract tests. **Blocked:** provider-specific cursoring, deduplication, coordinate/licensing fixtures, partial-failure matrices, and durable last-success health still require connector-by-connector fixtures and runtime telemetry.

- [blocked] **VCB-098 - Finish evidence-aware source depth.** Observed: arXiv PDF extraction contained a pass; podcasts/YouTube lack transcript-level citations; generic/Tavily wrappers mostly capture snippets/metadata. **Partial completion:** deep arXiv collection now retains the downloaded primary PDF and emits bounded page-coordinate observations with author-identity confidence; focused tests cover extraction metadata. **Blocked:** licensed transcript providers, speaker/timestamp extraction, source-specific licensing review, and durable provider cursor contracts remain unavailable for podcasts/YouTube and generic/Tavily sources.

- [x] **VCB-099 - Store valid deterministic JSON for JSON snapshots.** Observed: GitHub, arXiv, Product Hunt, HN, and YouTube used Python `str(dict/list)` while declaring `application/json`. **Completed:** all five connectors and research snapshot writers now use one shared canonical JSON serializer with UTF-8, sorted keys, compact separators, and valid JSON round-trip coverage; MIME remains `application/json`.

- [blocked] **VCB-100 - Replace or govern committed public-profile demo data.** Observed: the former seed README described 112 public people and the snapshot included pending-consent profile content/avatar bytes; the importer labelled non-submitted demos as inbound. **Partial completion:** the committed public-profile snapshot is removed; `make seed-demo` is the supported synthetic-only fixture; and the optional importer now persists `public_demo` references with an explicit policy version and operational-metrics exclusion marker. **Blocked:** source-by-source legal/owner review and a production retention/display/model-use policy are still required before public-reference importing can be treated as governed data.

- [blocked] **VCB-101 - Add automated dependency, image, secret, and license scanning.** Missing: CI security gates. **Partial completion:** `.github/workflows/security.yml` now audits locked npm/Python dependencies, inventories Python/Node licenses, builds both production images, scans them with Trivy, and scans repository history with Gitleaks; critical dependency findings and critical/high image findings fail the security jobs, while current known npm high findings are reported without being promoted to release-blocking critical findings. **Blocked:** completion requires an approved license allowlist/severity policy, time-bounded exception storage, and repository-admin configuration to require the security jobs before merge/release.

## P3 - Differentiated learning and workflow features

- [blocked] **VCB-102 - Define outcome labels without equating VC selection with founder merit.** **Partial completion:** Migration 029 adds append-only `opportunity_outcomes` records with separate sourcing/process/decision/longitudinal domains, typed outcome values, source provenance, observation time, horizon, censoring, and confidence; decision-domain records are explicitly selection signals rather than universal founder-success truth. **Blocked:** authoritative longitudinal sources, outcome collection workflows, censoring rules by metric, and a maintained label dictionary still require product/evaluation ownership and real post-decision data.

- [blocked] **VCB-103 - Calibrate scores and uncertainty empirically.** **Blocked:** no maintained labeled outcome set, time/entity-separated evaluation harness, lawful cohort policy, or approved calibration ownership exists; generating reliability/Brier or subgroup metrics from current synthetic/demo and investor-selection data would be misleading.

- [blocked] **VCB-104 - Measure marginal source value.** **Partial completion:** channel touches, artifact provenance, and domain-separated outcomes now provide persistence anchors for future source measurement. **Blocked:** source cost/latency ledgers, unique-claim attribution, correction labels, authoritative decisions, and exploration holdouts are not yet collected.

- [blocked] **VCB-105 - Add safe ranking experiments and exploration quotas.** **Blocked:** there is no offline replay dataset, ranker registry, shadow-result store, rollback control, or lawful cohort/exploration policy; live experimentation would conflate missing evidence with founder quality.

- [blocked] **VCB-106 - Build a channel-learning dashboard.** **Partial completion:** channel-touch and outcome tables preserve the future funnel’s source and outcome dimensions. **Blocked:** no query/source cost ledger, conversion/outcome coverage, correction data, confidence-interval service, or dashboard API contract exists; a UI now would fabricate metrics.

- [blocked] **VCB-107 - Add saved sourcing plans and change alerts.** **Partial completion:** the frontend preserves a transient URL-saved inbox view and the collector has a bounded Redis page tracker. **Blocked:** authenticated plan ownership, durable versioning, lawful refresh policy, change-diff events, alert delivery, and cost controls require the auth/tenant and operator-notification contracts.

- [blocked] **VCB-108 - Add IC collaboration and decision packet export.** **Partial completion:** memos and decision proposals already pin evidence, assessment, thesis, rubric, and artifact provenance snapshots. **Blocked:** authenticated comments/mentions/assignments, thread resolution, compare semantics, export format, and source-permission filtering have no current API or workspace contract.

- [blocked] **VCB-109 - Add founder-facing status and correction experiences.** **Partial completion:** public submissions are idempotent, evidence-aware, and retain correction/privacy guidance. **Blocked:** founder authentication/claim tokens, status permissions, correction review, opt-out propagation, and confidential/public response boundaries require the missing auth and rights-management contracts.

- [blocked] **VCB-110 - Expand the graph into evidence-backed sourcing intelligence.** **Partial completion:** Person, Organization, Relationship, evidence provenance, and channel-touch foundations exist. **Blocked:** Project/Event/Channel/Investor node schemas, valid-time graph APIs, evidence-linked edge contracts, weak-inference presentation, and graph-specific exploration evaluation remain unimplemented.

## Verification baseline from this audit

- Frontend ESLint: passed.
- Frontend TypeScript: passed.
- Frontend Vitest: 83 tests passed.
- Frontend production build: passed; current bundle is 407.76 kB JavaScript raw / 137.57 kB gzip and 69.73 kB CSS raw (entry JavaScript 218.56 kB raw / 71.43 kB gzip).
- Frontend bundle budget: passed.
- Backend pytest: 254 tests passed with one Starlette/httpx deprecation warning.
- Backend Ruff and configured mypy: passed.
- Alembic: one head, revision 031; migration 030 adds candidate-list cursor/filter indexes and migration 031 adds non-destructive collection persistence fingerprints.
- `git diff --check`: passed at the milestone boundary.
- Docker Compose `make check`: not available in this environment because the Docker daemon is not running; container-level validation remains outstanding.
- Earlier live browser QA findings remain historical evidence from the 2026-08-02 audit; the focused frontend regressions for failure states, polling, pagination, and mutation locking are covered by the current test suite.

## Progress notes

- 2026-08-02: VCB-018 frontend validation passed. `make check` could not start because Docker Compose could not connect to the local Docker daemon; no repository check was executed by the target container.
- 2026-08-02: Docker became available; the VCB-042 focused tests and full local checks passed, `make check` passed in Compose, and `make migrate` advanced the local database from e093be299381 to migration 015. The two pre-existing migration E501 failures were formatted in the VCB-042 checkpoint. The remaining VCB-041 blocker is schema/duplicate policy, not Docker access.
- 2026-08-02: VCB-043 focused and full validation passed; `make check` passed in Compose with 149 backend and 34 frontend tests, and `make migrate` advanced the local database to migration 016. Durable quarantine remains explicitly blocked on VCB-051.
- 2026-08-02: VCB-044 focused and full backend validation passed with 150 tests. Search-synthesized claims are now excluded from memo inputs; original-page verification remains the explicit blocker.
- 2026-08-02: VCB-045 focused and full backend validation passed with 151 tests. Tavily URL seeds route through the web collector, while entity leads remain discovery-only; provider-backed end-to-end verification remains blocked without credentials.
- 2026-08-02: VCB-048 focused and full backend validation passed with 152 tests. Uncited recent arXiv authors remain discoverable; citation metadata is explicitly non-gating. Cold-start coverage metrics remain blocked on durable discovery-event measurement.
- 2026-08-02: VCB-049 focused and full backend validation passed with 159 tests. Website collection now has SSRF, redirect, async Tavily, and byte-limit guardrails; remaining connector hardening is explicitly blocked as follow-up scope.
- 2026-08-02: VCB-050 focused and full backend validation passed with 160 tests. Collector errors now expose retry classification; durable retry/DLQ behavior remains blocked on VCB-051.
- 2026-08-02: VCB-051 foundation validation passed with 162 tests; local migration 017 adds the durable job ledger and discovery triggers now return status-trackable job IDs. Remaining job integrations and cancel/retry semantics are explicitly blocked scope.
- 2026-08-02: VCB-052 focused and full backend validation passed with 163 tests. Dispatcher enqueue failures now requeue work with preserved priority and refund Tavily reservations; crash-safe leases and atomic orchestration remain blocked scope.
- 2026-08-02: VCB-053 focused and full backend validation passed with 164 tests. Tavily budget state now survives worker restarts; actual usage reconciliation remains blocked scope.
- 2026-08-02: VCB-054 focused and full backend validation passed with 164 tests. Investigation gating now requires opportunity-scoped axes instead of any historical founder score; full per-run output identity remains blocked scope.
- 2026-08-02: VCB-060 backend/frontend validation passed with 164 backend and 34 frontend tests; migration 018 adds append-only candidate feedback and dismissal now persists a required reason before hiding the card. Save/assign/defer/undo and authenticated actor identity remain blocked scope.
- 2026-08-02: VCB-063 initial frontend checkpoint replaced the memo/research one-shot timers with bounded polling; later slices moved those views to durable JobRun IDs and terminal-aware polling (see 2026-08-07).
- 2026-08-02: VCB-069 frontend validation passed with 35 tests, lint, typecheck, and production build. Investigated now distinguishes null scores from measured zero; remaining profile/decision audit remains blocked scope.
- 2026-08-02: VCB-089 migration/seed wiring passed Compose config validation and focused demo-fixture tests. The migration service is ordered before API/worker readiness, and `make seed-demo` is documented as opt-in synthetic data; clean-volume acceptance remains blocked to avoid destroying the existing local database volume.
- 2026-08-02: VCB-090 configuration validation passed Compose config, six focused settings tests, live API startup, mypy, and full `make check` with 180 backend and 42 frontend tests. Backend settings now use `APP_*` consistently across `.env.example`, local settings, and the API/worker Compose environment; deployment secret injection and a full environment matrix remain blocked scope.
- 2026-08-02: VCB-092 focused lifecycle cleanup tests passed for API and worker shutdown; full shared AI/Redis ownership remains explicitly blocked pending a client-lifecycle design.
- 2026-08-02: VCB-099 canonical JSON serialization passed focused round-trip/stability coverage and source audit; all `application/json` snapshot writers now emit valid deterministic UTF-8 JSON.
- 2026-08-02: VCB-095 documentation review passed; README and architecture status now match the current repository, and the clean/demo/reset/troubleshooting/workflow paths are documented.
- 2026-08-02: VCB-101 security workflow added and locally exercised: npm critical-level audit (two known high advisories reported), pip-audit (no known vulnerabilities), Gitleaks (no leaks), license inventory, and both production image builds passed. The workflow blocks critical dependency/image findings; governance and allowlist decisions remain blocked.
- 2026-08-02: VCB-091 readiness tests passed for PostgreSQL/Redis/MinIO/worker-heartbeat success and failure paths; structured telemetry and dashboards remain explicitly blocked scope.
- 2026-08-03: VCB-084 frontend display-metric consolidation passed focused tests (16 tests), ESLint, TypeScript, and production build; remaining trend/status/confidence view-model work and the residual component audit remain explicitly blocked scope.
- 2026-08-03: VCB-083 decision-detail split passed focused decision tests (6 tests), ESLint, and TypeScript; the route container is 54 lines, while remaining oversized onboarding, founder-profile, and candidate-card modules remain explicitly blocked scope.
- 2026-08-03: VCB-083 onboarding split passed focused helper tests (2 tests), ESLint, TypeScript, and production build; the page container is 42 lines, while founder-profile and candidate-card extraction remain explicitly blocked scope.
- 2026-08-03: VCB-083 founder-profile split passed ESLint, TypeScript, and production build; the page container is 60 lines, while candidate-card extraction remains explicitly blocked scope.
- 2026-08-03: VCB-083 candidate-card split passed its 9-test behavior suite, ESLint, and TypeScript; all four oversized frontend modules now have feature-local containers/components and the item is complete pending the full checkpoint gate.
- 2026-08-03: VCB-096 demo workflow seed passed fixture tests and live Docker verification: reruns reused two demo IDs, candidates exposed investigating/memo-ready states, two scores/thesis matches, six assessments, one contradiction, and one succeeded five-section template memo; clean-volume and rendered browser walkthrough coverage remain blocked scope.
- 2026-08-03: VCB-097 connector contract slice passed 10 focused tests, Ruff, and mypy; all 10 registry entries now have readiness metadata and the persistence boundary validates connector output shape, while provider-specific fixtures and last-success telemetry remain blocked scope.
- 2026-08-03: VCB-094 operations runbook added with local PostgreSQL/MinIO backup-restore procedure, migration and worker-compatibility guidance, readiness checks, incident checklist, and production-drill acceptance criteria; production RPO/RTO and exercised recovery remain blocked scope.
- 2026-08-03: VCB-093 production-image slice passed the backend non-root Dockerfile change review and now makes `make build` target both production stages explicitly; network, secrets, image pinning, and runtime hardening remain blocked deployment scope.
- 2026-08-03: VCB-086 bundle-budget slice added a build-time raw/gzip gate for total JavaScript, entry JavaScript, and CSS, wired into `make check` and documented in README/AGENTS; product-approved budgets and per-route trend reporting remain blocked scope.
- 2026-08-03: VCB-085 completed across dead code, unused dependencies, and generated config drift; clean-container `npm ci`, the existing dependency audit workflow, and the full Docker validation gate passed. `npm audit` still reports two high-severity advisories under the workflow's critical-only failure threshold.
- 2026-08-03: VCB-065 dialog-accessibility slice added focus containment, Escape close, focus restoration, and three focused OutreachComposer tests; route-wide semantics and rendered device/screen-reader QA remain blocked scope.
- 2026-08-03: VCB-087 test-coverage slice added a rendered no-opportunity decision guard regression test alongside the OutreachComposer keyboard suite; clean service integration, browser E2E, and abuse coverage remain blocked scope.
- 2026-08-03: VCB-064/065 shell-accessibility slice added keyboard-visible skip navigation, a mobile workspace menu, one-main-landmark semantics, and three focused shell tests; rendered device and full route-flow QA remain blocked scope.
- 2026-08-03: VCB-065 card-semantics slice removed CandidateCard's pointer-only article navigation and added an explicit-action regression test; nested inbound-row semantics and rendered accessibility QA remain blocked scope.
- 2026-08-03: VCB-068 initial mutation-feedback slice added retryable discovery error state, query-label semantics, and focused rendered tests; full candidate cache coordination was completed in the 2026-08-07 mutation slice, while broader mutation families remain blocked.
- 2026-08-03: VCB-028 reviewed-outreach slice routed Investigated contact into the editable composer, added provenance/consent/suppression safeguards and explicit handoff approval, and covered the rendered route/composer behavior; persisted approval and provider delivery/reply state remain blocked on the outreach/auth/provider contract.
- 2026-08-03: VCB-066 identity-truth slice removed fabricated Sophie/workload UI values, labeled the shell as a Demo workspace, neutralized decision attribution, and updated outreach signing; authenticated workspace identity and audit actor mapping remain blocked on VCB-002.
- 2026-08-03: VCB-046 entity-boundary slice classified discovery seeds with conservative type/confidence priors, reported entity counts, and prevented non-person/low-confidence seeds from creating founder rows or scores; durable entity models and verification remain blocked on the entity-resolution schema.
- 2026-08-03: VCB-036 query-plan slice added visible clause parsing, geography forwarding status, missing-location correction, and downstream-intent disclosure with focused coverage; backend source-specific compilation and per-result explanations remain blocked on a discovery-plan contract.
- 2026-08-03: VCB-056 thesis-editor slice removed the hard-coded thesis name and preserved existing persisted thesis dimensions during save; complete dimension editing and immutable history operations remain blocked on the version-history/auth contract.
- 2026-08-03: VCB-057 thesis-scoring benchmark slice added labeled supported, excluded, and missing-evidence cases with typed criterion/evidence assertions; semantic similarity, analyst judgment, and calibrated evaluation remain blocked on model/evaluation ownership.
- 2026-08-03: VCB-062 profile-insights slice rendered the existing score history, evidence timeline, coverage/gaps, affiliations, and relationship semantics with focused component coverage; correction history and richer provenance remain blocked on API/auth contracts.
- 2026-08-03: VCB-061 memo-provenance slice preserved observation/claim IDs in the candidate API and added accessible memo disclosures for available source coordinates, timestamps, Trust intervals, and explanations; richer evidence graph metadata remains blocked on the expanded contract.
- 2026-08-03: VCB-067 initial candidate-list slice fixed latest-opportunity filter semantics, deterministic person tie ordering, canonical stage validation, and shared-session query safety; cursor pagination/search/envelope were still open at that checkpoint and landed in the 2026-08-07 v1 slice.
- 2026-08-03: VCB-047 Product Hunt slice applied the topic filter, followed GraphQL `pageInfo.endCursor`, and added two-page no-skip/no-duplication tests; reusable cursor state remains blocked on the shared discovery contract.
- 2026-08-03: VCB-037 sourcing-ranking slice removed the three-axis average, ranked complete profiles by their weakest independent axis, separated incomplete evidence into a discovery queue, and added ranking-rationale coverage; thesis-specific calibration remains blocked.
- 2026-08-03: VCB-024 inbound orchestration slice stopped premature memo enqueue, recorded missing triage outputs, and gated memo queueing on scoped claims, Founder Score, and all three opportunity axes; per-stage run/failure ledger and canonical diligence contract remain blocked.
- 2026-08-03: VCB-025 SLA-clock slice added Migration 020, idempotent 24-hour receipt/deadline initialization, decision-time met/breached event metadata, API countdown/risk fields, and truthful Decision Detail status; authenticated ownership, canonical stage pause/resume, alert delivery, and p50/p90 reporting remain blocked.
- 2026-08-03: VCB-026 canonical-lifecycle slice added the versioned `unified-v2` state machine, API contract, staged inbound worker transitions, domain vocabulary reuse, and workflow-diagram contract coverage; authenticated outreach ownership/provider transitions remain blocked in VCB-028/VCB-025.
- 2026-08-03: VCB-027 decision-proposal slice added Migration 021, deterministic proposal/readiness construction from memo and assessment records, candidate-detail exposure, human override persistence, and frontend consumption; investor-entered capital/conviction fields, complete provenance, and authenticated actors remain blocked.
- 2026-08-03: VCB-029 privacy-gate slice added Migration 022, versioned deny-by-default external AI policy metadata, per-purpose consent checks, direct-identifier redaction, and scoring/research/embedding/memo/outreach gating with safe local fallback; authenticated rights workflows and production provider policy remain blocked.
- 2026-08-03: VCB-031 source-policy slice added Migration 023, versioned snapshot use decisions, deny-by-default connector model use, explicit founder-provided/internal policies, and observation/scoring quarantine for unknown or disallowed material; source-owner/legal grants remain blocked.
- 2026-08-03: VCB-032 organization slice added Migration 024, deterministic organization linkage for new opportunities, temporal founder-role fields, and candidate-detail organization IDs; historical backfill/merge, team APIs, and company-scoped research remain blocked.
- 2026-08-03: VCB-033 evidence-link slice added Migration 025 and reconciliation writes for FK-backed claim/observation links while preserving legacy JSON compatibility; historical backfill, typed subject unions, and wrong-scope constraints remain blocked.
- 2026-08-03: VCB-034 artifact-provenance slice added Migration 026, a shared versioned metadata contract, and writer coverage for reconciliation, thesis/agent scoring, research assessments, memos, and decision proposals; historical backfill, billing telemetry, and relationship-run coverage remain blocked.
- 2026-08-03: VCB-038 activation-priority slice added `activation-priority-v1` with explicit missing-signal coverage and discovery queue integration; lifecycle/SLA policy, exploration quotas, and labeled offline ranking evaluation remain blocked.
- 2026-08-03: VCB-039 cold-start slice added Migration 027, bounded optional direct-work evidence fields on inbound submissions, opportunity-scoped observations for processing, and founder-facing missing-history guidance; authenticated correction/review, repeat milestones, and calibration remain blocked.
- 2026-08-03: VCB-040 fairness audit confirmed no lawful cohort labels, outcome labels, shadow-ranker surface, or approved proxy policy exists; the item is explicitly blocked to avoid inferring protected characteristics or claiming unsupported fairness metrics.
- 2026-08-03: VCB-055 scheduler slice added decision-deadline ordering, deterministic tie-breaks, PostgreSQL `SKIP LOCKED`, and a needs-attention count for budget-blocked pipeline work; stage budgets, cursor fairness, and durable operator queues remain blocked.
- 2026-08-03: VCB-058 inbox slice added the `/inbox` unified candidate view, URL-saved filters, stage/source/SLA/action/blocker presentation, and mobile/route wiring; durable ownership, exact opportunity ancestry, and server-side saved views remain blocked.
- 2026-08-03: VCB-059 channel-attribution slice added Migration 028 and append-only inbound/discovery touch persistence with source/query/ref metadata; conversion, cost, outcome labels, and funnel metrics remain blocked.
- 2026-08-03: VCB-100 data-governance slice removed the committed public-profile PostgreSQL snapshot, made the synthetic seed the supported demo path, and isolated the optional public importer as `public_demo` with an explicit metrics-exclusion policy; source-owner/legal review remains blocked.
- 2026-08-03: VCB-098 evidence-depth slice retained deep arXiv PDFs and added bounded page-coordinate observations with author-identity confidence; transcript/timestamp providers, licensing, and durable cursors remain blocked for podcast, YouTube, and generic/Tavily sources.
- 2026-08-03: VCB-102 outcome-contract slice added Migration 029 and append-only domain-separated outcome records with provenance, horizon, censoring, and confidence; authoritative longitudinal collection and label governance remain blocked.
- 2026-08-03: VCB-103–VCB-110 audit marked calibration, source-value measurement, ranking experiments, channel learning, saved plans/alerts, IC collaboration/export, founder rights, and graph intelligence blocked where the required evaluation data, auth, legal, or domain contracts do not exist; no unsupported metrics or product claims were added.
- 2026-08-03: VCB-006 and VCB-030 audit marked activation-token identity binding and rights-management workflows blocked on the missing authenticated principal, tenant, and lawful-erasure contracts; no unsafe email-only matching or destructive deletion was introduced.
- 2026-08-07: VCB-051/052/063 bounded async-lifecycle slice integrated durable JobRun IDs, queued/running/succeeded/failed status, attempt/result/error metadata, and `updated_at` polling data into research, scoring, memo, and discovery queue entry points; memo degradation is represented in `result.status=degraded` while the JobRun status is failed. Research/scoring/memo duplicate deliveries are terminal-safe; API queue-entry failures mark jobs failed, while dispatcher enqueue failures requeue/refund and remain queued. Frontend discovery, research, and memo views poll those runs and stop on terminal states. Cancellation is model-supported but not wired; processing, parsing, identity, avatar, and other non-integrated jobs, lease-based crash recovery, generic cancellation/retry orchestration, and full cross-workflow cache coordination remain blocked scope.
- 2026-08-07: VCB-067/073 candidate-list v1 slice added indexed cursor-bound server search, authoritative totals, a versioned envelope with legacy compatibility, and explicit inbound inbox Load more pagination with URL-persisted search. VCB-072 applied shared typed API failure handling across frontend reads and mutations; VCB-068 now invalidates candidate list/detail cache variants after supported mutations and locks decision actions during saves. Broader sort/saved-view/global-KPI, backend error-code, and non-candidate mutation contracts remain blocked.
- 2026-08-07: VCB-041 persistence-integrity slice added Migration 031 nullable snapshot/observation fingerprints, partial unique indexes for newly fingerprinted rows, deterministic legacy backfill that preserves duplicate evidence, and savepoint/re-read handling for concurrent unique races. Focused collector persistence tests (10), full backend tests (254), Ruff, mypy, and Alembic offline generation passed. Legacy duplicate review and a policy-approved non-null cleanup remain blocked.

## First practical milestone

Do not start by adding more connectors. A strong first milestone is:

1. VCB-001, VCB-002, VCB-004, VCB-005, VCB-008, VCB-018.
2. VCB-009 through VCB-015.
3. VCB-024 through VCB-027 and VCB-051 through VCB-055.
4. One deterministic E2E test proving: approved outbound lead -> traceable submission -> triage -> persistent Founder Score -> three independent opportunity axes -> claim-backed memo -> authenticated human decision inside the SLA.

That slice would convert the current hackathon demonstration into an honest, testable product foundation.
